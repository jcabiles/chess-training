# Stockfish Analysis Board — Python deps (open-source, free)
# NOTE: plain `uvicorn` (NOT uvicorn[standard]) to avoid uvloop/httptools
# wheels that may not build on very new CPython (e.g. 3.14). Target 3.12+.
fastapi>=0.110
uvicorn>=0.29
chess>=1.10
pydantic>=2.6

# test-only
pytest>=8.0
httpx>=0.27        # FastAPI TestClient transport
